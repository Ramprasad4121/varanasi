import { ComingSoon } from "@/components/ComingSoon";

export default function GoldPage() {
  return (
    <ComingSoon
      title="Gold-Backed Coin"
      eyebrow="Real-world assets"
      what="A coin backed by verified gold reserves — proof of reserve you can check, not a promise you have to take. We will never be our own custodian; the section opens when the reserve partnership is live."
      image="/images/trophy.jpg"
    />
  );
}

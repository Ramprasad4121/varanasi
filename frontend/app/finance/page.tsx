import { ComingSoon } from "@/components/ComingSoon";

export default function FinancePage() {
  return (
    <ComingSoon
      title="Community Finance"
      eyebrow="Finance"
      what="Savings, group pools, loans, and collateral — programmable money your agent can steward inside the same mandate rails. The contracts are written; the section opens when the first brick they depend on holds."
      image="/images/scales.jpg"
    />
  );
}
